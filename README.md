# 🤖 Lab 03: n8n LINE Gold Price Bot

## 📋 ข้อมูลทั่วไป

| รายการ | รายละเอียด |
|--------|------------|
| **วิชา** | n8n Workflow Automation |
| **สัปดาห์** | 3 - LINE Messaging API & External API Integration |
| **คะแนนเต็ม** | 100 คะแนน |
| **เวลา** | 3 ชั่วโมง |
| **Prerequisite** | Lab 01, Lab 02 |

## 🎯 วัตถุประสงค์การเรียนรู้

หลังจากทำ Lab นี้เสร็จ นักศึกษาจะสามารถ:

1. สร้าง LINE Messaging API Channel ได้
2. ใช้ ngrok สร้าง HTTPS Tunnel สำหรับ Webhook ได้
3. รับและประมวลผลข้อมูลจาก LINE Webhook ได้
4. เรียก External API ด้วย HTTP Request Node ได้
5. ส่งข้อความตอบกลับผ่าน LINE Reply API ได้
6. จัดการ Flow แบบมีเงื่อนไข (IF Node) ได้

## 📝 โจทย์

สร้าง **LINE Bot ราคาทอง** ที่ทำหน้าที่:

1. รับข้อความจากผู้ใช้ผ่าน LINE
2. ตรวจสอบว่าเป็นคำสั่งถามราคาทองหรือไม่ (`ราคาทอง`, `gold`, `ทอง`)
3. เรียก Thai Gold API ดึงราคาทองล่าสุด
4. จัดรูปแบบข้อความให้อ่านง่าย
5. ส่งข้อความตอบกลับผ่าน LINE Reply API

## 🌐 API ที่ใช้

| API | URL | Method | Auth |
|-----|-----|--------|------|
| **Thai Gold API** | `https://api.chnwt.dev/thai-gold-api/latest` | GET | ไม่ต้อง |
| **LINE Reply API** | `https://api.line.me/v2/bot/message/reply` | POST | Bearer Token |

## 📥 Thai Gold API Response

```json
{
  "status": "success",
  "response": {
    "update_date": "03/02/2569",
    "update_time": "เวลา 16:05 น. (ครั้งที่ 58)",
    "price": {
      "gold": {
        "buy": "71,631.00",
        "sell": "74,100.00"
      },
      "gold_bar": {
        "buy": "73,100.00",
        "sell": "73,300.00"
      }
    }
  }
}
```

## 📤 Expected LINE Reply Message

```
📊 ราคาทองวันนี้
━━━━━━━━━━━━━━━
📅 03/02/2569 เวลา 16:05 น.

🏆 ทองคำแท่ง 96.5%
   💰 ขายออก: 73,300.00 บาท
   💵 รับซื้อ: 73,100.00 บาท

💍 ทองรูปพรรณ 96.5%
   💰 ขายออก: 74,100.00 บาท
   💵 รับซื้อ: 71,631.00 บาท

📌 ข้อมูลจากสมาคมค้าทองคำ
```

## 🔧 Workflow Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              n8n Workflow                                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│  │ Webhook  │───▶│    IF    │───▶│  HTTP    │───▶│   Code   │───▶│  HTTP    │  │
│  │ (LINE)   │    │(คำสั่ง?)  │    │ Request  │    │ (Format) │    │ Request  │  │
│  │          │    │          │    │(Gold API)│    │          │    │(LINE API)│  │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘  │
│       ▲                                                               │         │
│       │                         ngrok tunnel                          │         │
│       │         https://xxxx.ngrok-free.app/webhook/gold-bot          │         │
└───────┼───────────────────────────────────────────────────────────────┼─────────┘
        │                                                               │
        │                    LINE Platform                              │
        │                                                               ▼
   ┌─────────┐                                                    ┌─────────┐
   │  User   │◀───────────────────────────────────────────────────│  User   │
   │ (ส่งมา) │                                                    │(ได้รับ) │
   └─────────┘                                                    └─────────┘
```

## 🔧 ขั้นตอนการทำ Lab

### Step 1: สร้าง LINE Messaging API Channel

⚠️ **อัพเดท 2026:** ตอนนี้ LINE ไม่อนุญาตให้สร้าง Messaging API channel โดยตรงแล้ว ต้องสร้าง LINE Official Account ก่อน แล้วค่อยเปิดใช้งาน Messaging API

#### 1.1 สร้าง LINE Official Account

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. **Login** ด้วย LINE Account
3. **สร้าง Provider** ใหม่ (ถ้ายังไม่มี):
   - คลิก **"Create a new provider"**
   - กรอกชื่อ Provider เช่น `MyCompany` หรือ `StudentLab`
   - คลิก **"Create"**
4. คลิกเข้าไปที่ Provider
5. คลิก **"Create a Messaging API channel"**
6. คลิกปุ่มสีเขียว **"Create a LINE Official Account"** (จะพาไปยังหน้าภายนอก)

#### 1.2 กรอกข้อมูล LINE Official Account

ในหน้าสร้าง LINE Official Account (ฟอร์มภาษาไทย):

**ข้อมูลบุคคล** (กรอกอัตโนมัติจากบัญชี LINE)
- ชื่อผู้ใช้: [ชื่อจากบัญชี LINE ของคุณ]
- ประเทศที่ใช้บริการ: ไทย

**ข้อมูลบัญชี** (ต้องกรอกเอง)

| ฟิลด์ | ตัวอย่าง | คำอธิบาย |
|------|----------|----------|
| **ชื่อบัญชี** | `Gold Price Bot` หรือ `บอทราคาทอง` | ชื่อที่แสดงใน LINE (สูงสุด 20 ตัวอักษร) |
| **ยี่นตล** | (เว้นว่างได้) | Optional - ชื่อแสดงเพิ่มเติม |
| **ประเภทธุรกิจ & ธุรกิจ** | `การเงินและประกันภัย` | เลือกจาก dropdown |
| **ชื่อบริษัท/ธุรกิจ** | `Student Lab` หรือเว้นว่าง | Optional |
| **ประเภทธุรกิจ** (รายละเอียด) | `บริการทางการเงินอื่นๆ` | เลือกหมวดย่อยจาก dropdown ที่ 2 |

**ข้อมูลติดต่อ** (เลื่อนลงด้านล่าง)
- **อีเมล**: กรอก email ของคุณ

**ข้อกำหนด**
- ✅ ติ๊กถูก **"ฉันได้อ่านและยอมรับข้อกำหนดในการให้บริการ LINE Official Account"**

คลิก **"ต่อไป"** หรือ **"ถัดไป"**

#### 1.3 เปิดใช้งาน Messaging API

หลังจากสร้าง LINE Official Account เสร็จ จะพาไปยังหน้า **LINE Official Account Manager**:

1. คลิก **⚙️ Settings** ที่มุมขวาบน
2. ไปที่แท็บ **"Messaging API"** (อยู่ในเมนูบนสุด)
3. เลื่อนลงหาส่วน **"Messaging API"**
4. คลิกปุ่ม **"Enable Messaging API"** หรือ **"Link Messaging API"**
5. เลือก **Provider** ที่คุณสร้างไว้ใน LINE Developers Console
6. คลิก **"OK"** หรือ **"Confirm"**
7. ระบบจะสร้าง Messaging API channel ให้อัตโนมัติ และพาคุณไปยังหน้า Messaging API settings

#### 1.4 ดึง Channel Access Token

1. กลับไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Provider และ Channel ที่เพิ่งสร้าง
3. ไปที่แท็บ **"Messaging API"**
4. เลื่อนลงไปหาส่วน **"Channel access token (long-lived)"**
5. คลิก **"Issue"** หรือ **"Reissue"**
6. **Copy** Token เก็บไว้ (จะใช้ใน n8n)

#### 1.5 ตั้งค่า Response settings (สำคัญมาก!)

ยังอยู่ในหน้า **LINE Official Account Manager**:

1. คลิกเมนู **"Response settings"** ที่เมนูซ้าย (ใน Settings)
2. ในหน้า **Toggle responses** ปรับสวิตช์ดังนี้:

| การตั้งค่า | เปลี่ยนเป็น | วิธีทำ |
|-----------|------------|--------|
| **Chat** | OFF ⚪ | (ปล่อยไว้ตามเดิม) |
| **Greeting message** | OFF ⚪ | คลิกสวิตช์ให้เป็นสีเทา (ปิด) |
| **Webhooks** | **ON** 🟢 | **คลิกสวิตช์ให้เป็นสีเขียว (เปิด)** ← สำคัญที่สุด! |
| **Auto-response messages** | OFF ⚪ | คลิกสวิตช์ให้เป็นสีเทา (ปิด) |

3. การตั้งค่าจะ **บันทึกอัตโนมัติ** ไม่ต้องกด Save

**เหตุผล:**
- ปิด **Greeting/Auto-response** เพื่อไม่ให้ Bot ตอบข้อความเอง
- เปิด **Webhooks** เพื่อให้ LINE ส่งข้อมูลข้อความมาที่ n8n

#### 1.6 เพิ่ม Bot เป็นเพื่อน

กลับไปหน้า Messaging API ใน LINE Developers Console:

1. เลื่อนลงไปหาส่วน **"Bot information"**
2. สแกน **QR code** ด้วยมือถือเพื่อเพิ่มเป็นเพื่อน

> 📖 ดูภาพประกอบและรายละเอียดเพิ่มเติมที่ [SETUP_GUIDE.md](SETUP_GUIDE.md)

### Step 2: ติดตั้งและรัน ngrok

1. สมัครบัญชีที่ [ngrok.com](https://ngrok.com)
2. ดาวน์โหลดและติดตั้ง ngrok
3. เพิ่ม authtoken:
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```
4. รัน ngrok:
```bash
ngrok http 5678
```
5. จด URL ที่ได้ เช่น `https://xxxx-xxx-xxx.ngrok-free.app`

### Step 3: รัน n8n

```bash
docker run -it --rm --name n8n -p 5678:5678 n8nio/n8n
```

### Step 4: สร้าง Workflow ใน n8n

#### 4.1 Webhook Node
- **HTTP Method**: POST
- **Path**: `gold-bot`
- **Response Mode**: `When Called Immediately` (เพื่อแก้ปัญหา 500 error เวลา verify webhook)

#### 4.2 IF Node (ตรวจสอบคำสั่ง)
- ตรวจสอบว่า message text มีคำว่า "ราคาทอง" หรือ "gold" หรือ "ทอง"

#### 4.3 HTTP Request Node (Gold API)
- **Method**: GET
- **URL**: `https://api.chnwt.dev/thai-gold-api/latest`

#### 4.4 Code Node (จัดรูปแบบข้อความ)

```javascript
const goldData = $input.first().json;
const lineEvent = $('Webhook').first().json.body.events[0];

const response = goldData.response;
const message = `📊 ราคาทองวันนี้
━━━━━━━━━━━━━━━
📅 ${response.update_date} ${response.update_time}

🏆 ทองคำแท่ง 96.5%
   💰 ขายออก: ${response.price.gold_bar.sell} บาท
   💵 รับซื้อ: ${response.price.gold_bar.buy} บาท

💍 ทองรูปพรรณ 96.5%
   💰 ขายออก: ${response.price.gold.sell} บาท
   💵 รับซื้อ: ${response.price.gold.buy} บาท

📌 ข้อมูลจากสมาคมค้าทองคำ`;

return [{
  json: {
    replyToken: lineEvent.replyToken,
    messages: [{
      type: "text",
      text: message
    }]
  }
}];
```

#### 4.5 HTTP Request Node (LINE Reply API)
- **Method**: POST
- **URL**: `https://api.line.me/v2/bot/message/reply`
- **Authentication**: Header Auth
  - **Name**: `Authorization`
  - **Value**: `Bearer YOUR_CHANNEL_ACCESS_TOKEN`
- **Body Content Type**: JSON
- **Body**: `{{ $json }}`

### Step 5: ตั้งค่า LINE Webhook URL

1. ไปที่ LINE Developers Console
2. เลือก Channel ของคุณ
3. ไปที่ **Messaging API** tab
4. ใส่ **Webhook URL**: `https://xxxx.ngrok-free.app/webhook/gold-bot`
5. เปิด **Use webhook**
6. คลิก **Verify** เพื่อทดสอบ

### Step 6: ทดสอบ Bot

#### 6.1 เตรียมความพร้อม

ก่อนทดสอบ ตรวจสอบให้แน่ใจว่า:

**✅ Checklist:**
- [ ] ngrok ยังทำงานอยู่ (Terminal ห้ามปิด)
  ```
  Session Status    online
  Forwarding        https://xxxx.ngrok-free.dev -> http://localhost:5678
  ```
- [ ] n8n ยังทำงานอยู่ (Terminal ห้ามปิด)
  ```
  Editor is now accessible via: http://localhost:5678/
  ```
- [ ] Workflow ใน n8n มีจุดสีเขียวบน Webhook node (กด "Test workflow" ถ้ายังไม่มี)
- [ ] LINE Response settings → **Webhooks = ON** 🟢 (ใน LINE Official Account Manager)
- [ ] Webhook URL ใน LINE Developers Console ตรงกับ ngrok URL
- [ ] เพิ่ม Bot เป็นเพื่อนแล้ว (สแกน QR Code จาก LINE Developers Console)

#### 6.2 ทดสอบส่งข้อความ

1. **เปิด LINE App** บนมือถือ
2. **ไปที่แชท Bot** ของคุณ (Bot basic ID: `@xxx-xxxxx`)
3. **ส่งข้อความ** ใดข้อความหนึ่ง:
   - `ราคาทอง` ✅
   - `ทอง` ✅
   - `gold` ✅

#### 6.3 สังเกตการทำงาน

**ใน n8n Browser:**
- คุณจะเห็น **แสงสีเขียววิ่งผ่าน nodes** ตามลำดับ:
  ```
  Webhook → Check Command → Get Gold Price → Format Message → LINE Reply
  ```
- ถ้าสำเร็จ ทุก node จะมี **checkmark สีเขียว** ✅

**ใน LINE App:**
Bot ควรตอบกลับด้วยข้อความแบบนี้:

```
📊 ราคาทองวันนี้
━━━━━━━━━━━━━━━
📅 04/02/2026 14:30:00

🏆 ทองคำแท่ง 96.5%
   💰 ขายออก: 45,000 บาท
   💵 รับซื้อ: 44,900 บาท

💍 ทองรูปพรรณ 96.5%
   💰 ขายออก: 45,400 บาท
   💵 รับซื้อ: 44,500 บาท

📌 ข้อมูลจากสมาคมค้าทองคำ
```

#### 6.4 ถ้าไม่ได้รับการตอบกลับ

**A) Bot ไม่ตอบเลย (workflow ไม่ทำงาน)**

สาเหตุ:
- ❌ Response settings → Webhooks ปิดอยู่
- ❌ Webhook URL ใน LINE ไม่ถูกต้องหรือ ngrok หมดอายุ
- ❌ n8n workflow ไม่ได้เปิดหรือไม่ได้กด "Test workflow"

วิธีแก้:
1. เช็ค LINE Official Account Manager → Response settings → **Webhooks ต้อง ON** 🟢
2. เช็ค LINE Developers Console → Messaging API → Webhook settings → **Use webhook ต้อง ON**
3. เช็ค ngrok ยังทำงานอยู่หรือไม่ ถ้า URL เปลี่ยนต้อง**อัพเดท Webhook URL ใหม่**
4. เช็ค n8n → กด "Test workflow" ให้ Webhook node มีจุดสีเขียว

**B) Workflow ทำงานแต่ Bot ไม่ตอบ (เห็น error ที่ LINE Reply node)**

สาเหตุ:
- ❌ Channel Access Token ไม่ถูกต้อง
- ❌ Header Authorization format ไม่ถูกต้อง

วิธีแก้:
1. คลิกที่ **LINE Reply node** ใน n8n
2. เช็ค Credential → Header Auth:
   - Name: `Authorization`
   - Value: `Bearer YOUR_CHANNEL_ACCESS_TOKEN`
   - ⚠️ ต้องมีคำว่า `Bearer` และเว้นวรรค 1 ช่องก่อน Token!

**C) ดู Execution Log**

1. ใน n8n คลิก **"Executions"** ที่แถบซ้าย
2. เลือก execution ล่าสุด
3. ดูว่า node ไหน **มี error (สีแดง)** ❌
4. คลิกที่ node นั้นเพื่อดู error message

#### 6.5 ทดสอบซ้ำ

หลังจากแก้ปัญหาแล้ว:
1. ส่งข้อความใหม่ไปที่ Bot อีกครั้ง
2. Bot ควรตอบกลับได้ปกติ ✅

### Step 7: Export และส่งงาน

1. ใน n8n คลิก Menu (⋮) > **Download**
2. บันทึกเป็น `workflow.json`
3. แทนที่ไฟล์ในโปรเจค
4. Commit และ Push:
```bash
git add workflow.json
git commit -m "Add LINE Gold Bot workflow"
git push origin main
```

## ✅ เกณฑ์การให้คะแนน (100 คะแนน)

| Test | คะแนน | ตรวจอะไร |
|------|-------|----------|
| มีไฟล์ workflow.json | 10 | ส่งไฟล์มา |
| เป็น valid JSON | 10 | Parse ได้ถูกต้อง |
| มี Webhook Node | 10 | รับข้อมูลจาก LINE |
| Webhook ใช้ POST method | 10 | ตั้งค่าถูกต้อง |
| มี HTTP Request Node สำหรับ Gold API | 10 | เรียก API ราคาทอง |
| Gold API URL ถูกต้อง | 10 | `api.chnwt.dev/thai-gold-api/latest` |
| มี Code Node | 10 | จัดรูปแบบข้อความ |
| มี HTTP Request Node สำหรับ LINE Reply | 10 | ส่งข้อความกลับ |
| LINE Reply URL ถูกต้อง | 10 | `api.line.me/v2/bot/message/reply` |
| มีการจัดรูปแบบข้อความราคาทอง | 10 | แสดง buy/sell price |
| **รวม** | **100** | |

## 📚 Resources

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [ngrok Documentation](https://ngrok.com/docs)
- [Thai Gold API](https://github.com/max180643/thai-gold-api)
- [n8n HTTP Request Node](https://docs.n8n.io/integrations/builtin/core-nodes/n8n-nodes-base.httprequest/)

## ⚠️ ข้อควรระวัง

1. **อย่าเผยแพร่ Channel Access Token** - เป็นข้อมูลลับ
2. **ngrok URL จะเปลี่ยนทุกครั้งที่รันใหม่** - ต้องอัพเดท LINE Webhook URL
3. **อย่าปิด Terminal** ที่รัน ngrok ระหว่างทำ Lab
4. **replyToken ใช้ได้ครั้งเดียว** - ใช้ตอบกลับได้แค่ 1 ครั้ง

## 🆘 Troubleshooting

### ❌ Error: 500 Internal Server Error (เวลา Verify Webhook)

**สาเหตุ:**
- Webhook Response Mode ตั้งเป็น "Last Node" → เวลา LINE ส่ง empty events (`[]`) มา verify จะเกิด error

**วิธีแก้:**
1. ไปที่ **Webhook node** ใน n8n
2. เปลี่ยน **Respond** section เป็น **"Immediately"** (หรือ `responseMode: "onReceived"`)
3. กด "Test workflow" อีกครั้ง
4. กลับไปกด "Verify" ใน LINE Developers Console → ควรได้ "Success" ✅

### ❌ Bot ไม่ตอบกลับเลย

**สาเหตุที่พบบ่อย:**

1. **Response settings → Webhooks ปิดอยู่**
   - แก้: LINE Official Account Manager → Response settings → เปิด Webhooks 🟢

2. **Webhook URL ใน LINE ไม่ถูกต้อง**
   - แก้: ตรวจสอบว่า URL ตรงกับ ngrok URL + path ที่ถูกต้อง
   - ตัวอย่าง: `https://xxxx.ngrok-free.dev/webhook/gold-bot`

3. **ngrok หมดอายุหรือปิดไป**
   - แก้: เช็ค Terminal ที่รัน ngrok ว่ายังทำงานอยู่หรือไม่
   - ถ้า URL เปลี่ยน ต้องอัพเดท Webhook URL ใน LINE ใหม่

4. **n8n Workflow ไม่ได้เปิด**
   - แก้: เปิด n8n → กด "Test workflow" → ต้องเห็นจุดสีเขียวบน Webhook node

### ❌ Workflow ทำงาน แต่ Bot ไม่ตอบ (Error 401 Unauthorized)

**สาเหตุ:**
- Channel Access Token ไม่ถูกต้อง หรือ
- Header Authorization format ไม่ถูกต้อง

**วิธีแก้:**
1. ไปที่ LINE Reply node → Settings → Credential
2. ตรวจสอบ Header Auth:
   - Name: `Authorization`
   - Value: `Bearer YOUR_CHANNEL_ACCESS_TOKEN`
   - ⚠️ **ต้องมีคำว่า "Bearer" และเว้นวรรค 1 ช่อง!**

### ❌ Check Command ไม่ match (ไม่ไปทาง true)

**สาเหตุ:**
- ส่งข้อความผิด (ต้องเป็น "ราคาทอง", "ทอง", หรือ "gold")
- ส่งข้อความประเภทอื่น (รูปภาพ, sticker)

**วิธีแก้:**
- ส่งเฉพาะ **ข้อความธรรมดา** (text) เท่านั้น
- พิมพ์ "ราคาทอง", "ทอง", หรือ "gold" ให้ถูกต้อง

### ❌ Get Gold Price API Error

**สาเหตุ:**
- API `https://api.chnwt.dev/thai-gold-api/latest` ไม่ตอบกลับ
- Internet connection ขาดหาย

**วิธีแก้:**
1. ลองเปิด URL `https://api.chnwt.dev/thai-gold-api/latest` ใน browser
2. ควรเห็น JSON response ของราคาทอง
3. ถ้าไม่ได้ แสดงว่า API down หรือ internet มีปัญหา

### 📋 วิธีดู Execution Log

1. ใน n8n คลิก **"Executions"** ที่แถบด้านซ้าย
2. คลิกที่ execution ล่าสุด
3. ดู node ไหนมี **สีแดง** ❌ (error)
4. คลิกที่ node เพื่อดู error message ละเอียด

---

**Good luck! 🍀**
